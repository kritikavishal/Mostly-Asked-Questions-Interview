class Solution {
    
    public ListNode reverseList(ListNode head) {
        ListNode curr=head,prev=null,nextnode=null;
        while(curr!=null){
            nextnode=curr.next;
            curr.next=prev;
            prev=curr;
            curr=nextnode;
        }
        return prev;
    }
}
